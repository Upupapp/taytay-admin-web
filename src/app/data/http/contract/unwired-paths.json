{
  "$comment": "Composed request paths this console builds that the API does not publish AT THAT VERB. EVERY ONE IS A 404 OR 405 IN THE HTTP CONFIGURATION. This file is a BASELINE, not an allow-list: check:routes prints the count on every run and fails if the list grows. Entries are 'VERB path' — the verb is part of the contract, and comparing paths alone passed a POST to a GET-only route. Regenerate with `node tools/check-routes.mjs --write-baseline`.",
  "recordedBy": "TAB 19",
  "recordedOn": "2026-08-19",
  "count": 16,
  "paths": [
    "GET admin/assistance-requests/advisory",
    "GET admin/cases",
    "GET admin/cases/queues",
    "GET admin/cases/{}",
    "GET admin/events/{}/registrants/export",
    "GET admin/privacy/corrections",
    "PATCH admin/assistance-intakes/{}",
    "POST admin/cases/{}/assignment",
    "POST admin/cases/{}/notes",
    "POST admin/cases/{}/status",
    "POST admin/cases/{}/tasks",
    "POST admin/cases/{}/tasks/{}/assignment",
    "POST admin/cases/{}/tasks/{}/complete",
    "POST admin/cases/{}/tasks/{}/schedule",
    "POST admin/families/transfers",
    "POST admin/reports/{}/export"
  ]
}
